# SARA small — ~45M params demo/showcase scale (GPU training recommended)
preset: small
rope_theta: 10000.0
qk_norm: true
tie_embeddings: true
sample_rate: 16000
n_mels: 64
n_fft: 512
hop_length: 160
n_note_steps: 32
max_tools: 32
max_agent_steps: 8
dtype: fp32